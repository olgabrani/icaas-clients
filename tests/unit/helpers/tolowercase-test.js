import { tolowercase } from '../../../helpers/tolowercase';
import { module, test } from 'qunit';

module('Unit | Helper | tolowercase');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = tolowercase(42);
  assert.ok(result);
});
